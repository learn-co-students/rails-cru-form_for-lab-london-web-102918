<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Artists</title>
  </head>
  <body>
    <h1>Artists:</h1>
    <ul>
      <% @artists.each do|a| %>
        <h1><%=link_to a.name, artist_path(a)%></h1>
        <p><%= a.bio %></p>
      <% end %>
    </ul>
  </body>
</html>
